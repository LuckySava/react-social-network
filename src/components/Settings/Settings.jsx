import s from './Settings.module.scss';

const Settings = () => {
    return (
        <div className={s.settings_wrapper}>
            <h2>Settings</h2>
        </div>
    )
}

export default Settings;